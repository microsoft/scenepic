steps:
- task: TwineAuthenticate@1
  displayName: 'Twine Authenticate '

- script: |
    pip install twine
    python -m twine --version
    twine upload --config-file $(PYPIRC_PATH) dist/*.whl
  
  displayName: 'Upload wheel'
  continueOnError: true
